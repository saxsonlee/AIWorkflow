# Decision

## 初始判断

### 本次用户诉求

初始化一个最小可运行的 AIWorkflow 工作区。

### 归类判断

- Topic：ExampleTopic
- Issue：ExampleIssue
- 是否新建 Issue：是
- 是否新建 Iteration：是

### 判断理由

这是模板自带的示例问题，用于证明工作区结构完整。

### 拟执行内容

- 使用 `aiworkflow_minimal` mode。
- 验证 `Current.json`、`Resolution.json` 和当前 Iteration 文件存在且引用一致。

### 用户确认状态

模板默认示例。
